<h3>Members</h3>

<table id="memberlist">
  <tr>
   <th>Name</td>
   <th>Email</td>
  </tr>

<% for member in @members %>
  <tr>
    <td><%= link_to h(member.name), member_path(member) %></td>
    <td><%= link_to h(member.email), "mailto:#{member.email}" %></td>
  </tr>
<% end %>
</table>

<p>
  <%= link_to 'Add new member', new_member_path %>
</p>
