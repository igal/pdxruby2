<li>
  <p>
    <b><%= link_to h(event.name), event_path(event) %></b>

    <br/>

    <% if event.starts_at %>
      <%= normalize_time(event.starts_at, event.ends_at) %>
    <% end %>

    <% if event.location %>
      at <%= h event.location.name %>
    <% end %>
  </p>
</li>
